import express from 'express'

const dataRouter = express.Router();

// dataRouter.use("/community", communityRouter)
// dataRouter.use("/showu", showuRouter)
// dataRouter.use("/shop", shopRouter)
// dataRouter.use("/vod", vodRouter)
// dataRouter.use("/reservation", reservationRouter)

export default dataRouter;